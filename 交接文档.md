# dev-git-hub 交接文档

> **定位**：dev-git-hub 独立项目 = 局域网 Git 基建单一信源（LAN 中枢 + Windows 副本 + WAN 灾备 + git 复杂远端操作工具）。
> **单一信源原则**：所有 git 基建方案/证据卡/评审报告/工具实现以 **dev-git-hub 为唯一权威**；DevProjectTeamSkill 不保留实现，仅经薄封装代理调用 + 引用登记。
> **接口共享**：DevProjectTeamSkill 经 `PROJECT_ROOT` 环境变量 + 薄封装代理调用本库工具，本库脚本以目标仓库为工作根读远端/台账。

---

## 🔴 L1 必读核心

### 项目速览
- **项目**：dev-git-hub（局域网 Git 基建独立项目）
- **授权**：AUTH-014（2026-08-30 用户采纳方案 A，长期有效至 2027-12-31）
- **版本**：初始 v0.1（2026-08-30）
- **上级协同**：DevProjectTeamSkill（技能库，仅登记引用不内嵌）

### 当前任务类型
- **任务类型**：git 基建完整交接 + 独立维护
- **任务边界**：承接 git 复杂远端操作（mirror_push/github_push/github_ip_refresh/check_github_connectivity/restore_github_push/_gh_ip_probe）+ LAN 中枢 + Windows 副本 + WAN 灾备 + 同步策略
- **输入依赖**：DevProjectTeamSkill 孵化的方案（docs/ 下 3 份方案 + 证据卡 + 评审报告）
- **输出预期**：独立仓库完整运行（hub 推送待 SSH 授权 + GitHub/Gitee 远端待创建）

### 关键风险/阻塞
- **阻塞 1**：SSH 密钥授权未完成（M2——`~/.ssh/authorized_keys` 未配置）→ hub(LAN) 推送不可用
- **阻塞 2**：GitHub/Gitee 侧 `gogojaja/dev-git-hub` 远程仓库未创建（404）→ origin/mirror 推送不可用
- **无风险**：本地仓库完整（初始提交 + 修复提交）

### 下一步唯一动作
- [ ] 用户创建 GitHub/Gitee `dev-git-hub` 空仓库 → 推 origin+mirror
- [ ] 用户完成 SSH 密钥授权（M2）→ 推 hub
- [ ] 补充 README 的「维护接口契约」段（项目间接口共享规范）

### 铁律锚点
- **核心**：授权 → 备份 → 留痕
- **隔离**：本库内容不混入 DevProjectTeamSkill；共享标准引用不复制；各自 .gitignore/台账/发布链
- **接口**：DevProjectTeamSkill 调用经薄封装代理（注入 PROJECT_ROOT），本库以目标仓库为工作根

---

## 🟡 L2 标准上下文

### 详细工作断点
- **最近交接**：2026-08-30（从 DevProjectTeamSkill 完整交接）
- **仓库状态**：git 仓库已初始化（main），初始提交 `50ff778` + 修复提交 `b2b390b`
- **工具承载**：6 件 git 工具 + `load_secret.py` + `references/github_access.md`（均单源在本库）
- **配置层**：`config.example.yaml`（本机 IP/路径解耦，真实 config.yaml 不入库）

### 进行中工作
- **M1 工具承接**：已完成（6 工具 + load_secret + github_access 单源）
- **M2 SSH 授权**：待用户执行（authorized_keys 配置）
- **M3 远端推送**：待 GitHub/Gitee 远程仓库创建后推 origin+mirror；hub 待 SSH
- **交接文档**：本文件建立中

### 关键文件索引
- `tools/mirror_push.py`：双推（PROJECT_ROOT 环境变量支持，以目标仓库为工作根）
- `tools/github_push.py`：GitHub 真实 IP 推送（同 PROJECT_ROOT 支持）
- `tools/github_ip_refresh.py` / `check_github_connectivity.py` / `restore_github_push.sh` / `_gh_ip_probe.py`
- `tools/load_secret.py`：凭据跨平台装载
- `references/github_access.md`：GitHub 访问异常处理标准
- `docs/`：方案/证据卡/评审报告（单源）
- `config.example.yaml`：本机配置层（解耦）

### 待办事项
1. 用户创建 GitHub/Gitee `dev-git-hub` 仓库并推送
2. 用户完成 SSH 密钥授权并推 hub
3. 验证 DevProjectTeamSkill 薄封装代理链路完整（--verify 已通过）
4. 维护接口契约段补入 README

---

## 🟢 L3 完整归档

### 历史交接记录
- **2026-08-30**：DevProjectTeamSkill 孵化 git 基建方案（局域网git服务器方案.md + 证据卡 + 评审报告）→ 用户采纳方案 A 建独立项目 → 完整交接（工具/标准/配置层/同步策略）→ 本库独立仓库建立。

---

**最后更新**：2026-08-30（完整交接建立）