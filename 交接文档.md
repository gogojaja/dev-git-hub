# dev-git-hub 交接文档

> **定位**：dev-git-hub 独立项目 = 局域网 Git 基建单一信源（三层备份拓扑 + git 复杂远端操作工具）。
> **单一信源原则**：所有 git 基建方案/证据卡/评审报告/工具实现以 **dev-git-hub 为唯一权威**；DevProjectTeamSkill 不保留实现，仅经薄封装代理调用 + 引用登记。
> **接口共享**：DevProjectTeamSkill 经 `PROJECT_ROOT` 环境变量 + 薄封装代理调用本库工具，本库脚本以目标仓库为工作根读远端/台账。

---

## 🔴 L1 必读核心

### 项目速览
- **项目**：dev-git-hub（局域网 Git 基建独立项目）
- **授权**：AUTH-014（2026-08-30 用户采纳方案 A，长期有效至 2027-12-31）
- **版本**：v0.2（2026-09-02 三层备份拓扑落地）
- **上级协同**：DevProjectTeamSkill（技能库，仅登记引用不内嵌）

### 三层备份拓扑（2026-09-02 落地）

```
工作层（本机 /Volumes/BR256G/*，11 个仓库）
    ↓ push（append-only，不 force）
局域网层（192.168.3.86:~/git/hub/*.git = 权威汇聚点）
    ↓ push（择机补推）
离站层（GitHub origin + Gitee mirror = 抗本地整体灾难）
```

- **恢复优先级**：hub（局域网，0.2s）→ mirror（Gitee）→ origin（GitHub）
- **铁律**：裸库只接受 append-only 普通 push，永不强推；恢复时用 `git clone` 重建工作副本
- **远程命名固化**：`hub`(局域网) / `origin`(GitHub) / `mirror`(Gitee)

### 当前状态
- **阻塞 1**：~~SSH 密钥授权未完成~~ ✅ 已完成（2026-09-02）
- **阻塞 2**：~~GitHub/Gitee 远程仓库未创建~~ ✅ 已创建并推送
- **无阻塞**：三层拓扑完整运行，`mirror_push.py` 默认推送 origin + mirror + hub

### 恢复流程

```bash
# 从 hub 恢复（局域网，最快）
git clone ssh://gogo@192.168.3.86/~/git/hub/<repo>.git

# 从 mirror 恢复（离站，兜底）
git clone https://gitee.com/gogojaja/<repo>.git

# 从 origin 恢复（离站，备用）
git clone https://github.com/gogojaja/<repo>.git
```

### 铁律锚点
- **核心**：授权 → 备份 → 留痕
- **隔离**：本库内容不混入 DevProjectTeamSkill；共享标准引用不复制；各自 .gitignore/台账/发布链
- **接口**：DevProjectTeamSkill 调用经薄封装代理（注入 PROJECT_ROOT），本库以目标仓库为工作根

---

## 🟡 L2 标准上下文

### 关键文件索引
- `tools/mirror_push.py`：三推（origin + mirror + hub，PROJECT_ROOT 环境变量支持）
- `tools/github_push.py`：GitHub 真实 IP 推送（同 PROJECT_ROOT 支持）
- `tools/github_ip_refresh.py` / `check_github_connectivity.py` / `restore_github_push.sh` / `_gh_ip_probe.py`
- `tools/load_secret.py`：凭据跨平台装载
- `references/github_access.md`：GitHub 访问异常处理标准
- `docs/`：方案/证据卡/评审报告（单源）
- `config.example.yaml`：本机配置层（解耦）

### 远端配置（所有仓库统一）

| 远程 | URL | 认证 | 用途 |
|---|---|---|---|
| origin | `https://github.com/gogojaja/<repo>.git` | GitHub PAT（real-IP 注入） | 离站灾备 |
| mirror | `https://gitee.com/gogojaja/<repo>.git` | Gitee token（insteadOf 注入） | 离站灾备 |
| hub | `ssh://gogo@192.168.3.86/~/git/hub/<repo>.git` | SSH 密钥 | 局域网权威汇聚点 |

### 凭据存放（铁律 #3 A 级）
- `.secrets/github_token`：GitHub fine-grained PAT
- `.secrets/gitee_token`：Gitee 32 位 token
- `.secrets/gitee_user`：`gogojaja`
- `~/.ssh/id_ed25519`：SSH 密钥（私钥不入库）

### 局域网 hub 主机
- **地址**：`192.168.3.86`
- **用户**：`gogo`
- **裸库路径**：`~/git/hub/<repo>.git`
- **磁盘**：460G 总量，197G 可用
- **裸库清单**（11 个，与本机仓库一一对应）：
  DevProjectTeamSkill、TwinForge、ai-assistant-system、dev-git-hub、dev-model-router、dev-project-mgmt、dev-security-tools、dev-task-scheduler、dev-test-tools、free-api-hub、lark-training-ppt-generator

### 待办事项
1. 收紧 `.86` SSH 配置：禁用口令认证，仅留密钥（需 sudo）
2. 定期恢复演练（建议每季度一次，结果写入 `台账/31_恢复演练记录.csv`）

---

## 🟢 L3 完整归档

### 历史交接记录
- **2026-08-30**：DevProjectTeamSkill 孵化 git 基建方案 → 用户采纳方案 A 建独立项目 → 完整交接（工具/标准/配置层/同步策略）→ 本库独立仓库建立。
- **2026-09-01**：修复 mirror_push/github_push：--git-force 强推 + 分叉分类 + 全真实 IP 兜底机制。
- **2026-09-02**：三层备份拓扑落地（SSH 密钥打通 → hub 裸库建立 → 11 仓库首推 → 恢复演练通过）；修复 URL 凭据注入健壮性（防止双凭据畸形 URL）；mirror_push 扩展默认推送 hub。

---

**最后更新**：2026-09-02（三层备份拓扑落地）
